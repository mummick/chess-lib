Move.isValid(field: IField): boolean
переделать Field.makeMove на Move.makeMove
Figure.toString() - переделать на маленькие-большие буквы в зависимости от цвета
Position.addFigure(coord, figure)
Position.deleteFigure(coord)
Position.addFigureStringCoord(scoord: string, figure: Figure)
Position.deleteFigureStringCoord(scoord: string)
ChessCoord.fromString(scoord: string): ChessCoord
Field.toFEN(): string
Field.fromFEN(fen: string): Field
убрать фигуру из модели данных Move